# Merge Conflict Resolution Report

1. **Conflict Trigger**: Merged branch `conflict-2` into `main` after `conflict-1` was already merged. Both branches modified the same line in `conflict-file.txt`.
2. **Resolution Process**: Opened `conflict-file.txt`, located the conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`), and manually selected the final code state.
3. **Finalization**: Staged the resolved file using `git add` and committed the merge.
